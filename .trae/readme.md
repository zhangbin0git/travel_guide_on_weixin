获取高德MCP工具详细数据结构

我要做一个旅行攻略的小程序，主要是基于高德 MCP 工具提供的数据来完成，但是我不知道这些工具返回的数据结构是什么，没办法进行需求定义。

请你先调用 MCP Server amap-sse 获取高德MCP工具详细数据结构，将结果写入 docs 目录下，这是官方文档 https://lbs.amap.com/api/mcp-server/summary 描述了 MCP Server amap-sse 提供的能力，也介绍调用该工具要传入的参数信息，你需要按照工具要求进行参数传递。

如果 MCP Server amap-sse 工具调用失败，可以尝试更换其他工具调用，有可能是参数传递的不对，这个 MCP Server 我在其它地方测试过是可以成功连接的，如果尝试多次一直失败，请停止并告诉我内容，一定不要自己假造数据。
