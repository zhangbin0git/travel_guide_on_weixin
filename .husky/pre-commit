#!/usr/bin/env sh

# 运行前端代码检查
echo "检查前端代码规范..."
cd frontend/travel-guide-app
npm run lint
if [ $? -ne 0 ]; then
  echo "前端代码检查失败，请修复后再提交"
  exit 1
fi

# 运行后端代码检查
echo "检查后端代码规范..."
cd ../../backend
npm run build
if [ $? -ne 0 ]; then
  echo "后端代码编译失败，请修复后再提交"
  exit 1
fi

echo "代码检查通过，可以提交"
